<div align="center"><img alt="Iskariot — AI systems engineer. I build the instruments the stack doesn't ship with itself. Inference serving internals and agent runtimes. Everything on this page is public, and every claim on it is one you can argue with." src="assets/01-hero.png" width="100%"><img alt="FIG. 01 — three regimes of the same serving engine. A radix prefix tree that grows and evicts, an in-flight batch that never drains between requests, speculative decode whose accept rate moves with the load, and a fleet that scales on queue depth rather than CPU. Cold, warm and under load, with the scheduler log underneath." src="assets/02-figs.gif" width="100%"><img alt="FIG. 02 — the desk. claude-quant across one unattended session: four acts on four instruments, four different formulas, three positions, one refusal and one loss. Every position traces to a DEC carrying the proof that produced its size; a position with no DEC cannot exist." src="assets/03-desk.gif" width="100%"><a href="https://github.com/Isk4R1oT?tab=repositories"><img alt="the shelf — vernier: records N runs at the model-API wire and names the first divergent step. edp: explicit decision protocol, constraints re-read every turn. ctx: htop for LLM prompts. vecdoctor: EXPLAIN ANALYZE for your vector store. claude-quant: a full agent system you can read end to end. assay, mcpgen and plinth: an eval harness hard to game, an MCP server generated from any API, and hosting that meters and bills black-box agents." src="assets/04-shelf.png" width="100%"></a><img alt="positions I'd defend, and the stack. On serving and inference: benchmark at the batch sizes you will actually serve; longest-prefix routing is a scheduling decision before it is a memory one; speculative decoding has no single speedup number; quantization is only free if you measured what it broke; the cheapest throughput came from the scheduler, not the kernel; same seed at temperature 0 is not reproducible unless batch composition is pinned; a GPU is a budget line, not a resource. On agent runtimes: decision drift is confident self-contradiction, not forgetting; an agent that cannot refuse itself is not safe to leave running; a number acted on should be computed in a sandbox, not asserted in prose; anything read through a tool is data and never acquires instruction privileges; long-horizon memory is an eviction policy wearing the costume of a database; a model upgrade is a regression event until golden tasks are replayed. The stack spans SGLang and vLLM, disaggregated prefill and decode, cache-aware routing, quantization and speculative decoding, the Claude Agent SDK, LangGraph, MCP and FastMCP, retrieval, evaluation, isolation, Kubernetes-native serving and classic ML. What a number has to survive: load tested at matched concurrency against a naive baseline, judged by a rubric calibrated on human reference, costed per 1M tokens at matched quality, and scored on a golden set that predates the change it judges." src="assets/05-close.png" width="100%">

<a href="https://t.me/Ig0Ro4"><img alt="telegram" src="assets/btn-telegram.png" height="40"></a>&nbsp;&nbsp;<a href="https://github.com/Isk4R1oT?tab=repositories"><img alt="all repos" src="assets/btn-repos.png" height="40"></a>

</div>
