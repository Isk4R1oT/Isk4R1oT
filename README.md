![Iskariot — AI systems engineer. I build the instruments the stack doesn't ship with itself. Inference serving internals and agent runtimes. Everything on this page is public, and every claim on it is one you can argue with.](assets/01-hero.png)

![FIG. 01 — three regimes of the same serving engine. A radix prefix tree that grows and evicts, an in-flight batch that never drains between requests, speculative decode whose accept rate moves with the load, and a fleet that scales on queue depth rather than CPU. Cold, warm and under load, with the scheduler log underneath.](assets/02-pipeline.gif)

![FIG. 02 — the desk. claude-quant across one unattended session: four acts on four instruments, four different formulas, three positions, one refusal and one loss. Every position traces to a DEC carrying the proof that produced its size; a position with no DEC cannot exist.](assets/03-desk.gif)

![the shelf — vernier: records N runs at the model-API wire and names the first divergent step. edp: explicit decision protocol, constraints re-read every turn. ctx: htop for LLM prompts. vecdoctor: EXPLAIN ANALYZE for your vector store. claude-quant: a full agent system you can read end to end. assay, mcpgen and plinth: an eval harness hard to game, an MCP server generated from any API, and hosting that meters and bills black-box agents.](assets/04-shelf.png)

**[vernier](https://github.com/Isk4R1oT/vernier)** · **[edp](https://github.com/Isk4R1oT/edp)** · **[ctx](https://github.com/Isk4R1oT/ctx)** · **[vecdoctor](https://github.com/Isk4R1oT/vecdoctor)** · **[claude-quant](https://github.com/Isk4R1oT/claude-quant)** · **[plinth](https://github.com/Isk4R1oT/Plinth)** · [everything else](https://github.com/Isk4R1oT?tab=repositories)

![positions I'd defend — on serving and inference: benchmark at the batch sizes you will actually serve; longest-prefix routing is a scheduling decision before it is a memory one; speculative decoding has no single speedup number; quantization is only free if you measured what it broke; the cheapest throughput came from the scheduler, not the kernel; same seed at temperature 0 is not reproducible unless batch composition is pinned; a GPU is a budget line, not a resource. On agent runtimes: decision drift is confident self-contradiction, not forgetting; an agent that cannot refuse itself is not safe to leave running; a number acted on should be computed in a sandbox, not asserted in prose; tool arguments are validated before dispatch; anything read through a tool is data and never acquires instruction privileges; long-horizon memory is an eviction policy wearing the costume of a database; a model upgrade is a regression event until golden tasks are replayed.](assets/05-positions.png)

![the stack — serving and inference: SGLang, vLLM, disaggregated prefill and decode, cache-aware routing, hierarchical KV offload, GPTQ, AWQ, INT8, FP8, speculative decoding, tensor and expert parallelism, FlashAttention, Triton, CUDA graphs, Nsight. Agents and tooling: Claude Agent SDK, LangGraph, MCP and FastMCP, PydanticAI, meta-agents, supervisor and fan-out orchestration, multi-model routing, code-execution-as-tool. Long-horizon memory, retrieval, evaluation, security and isolation, infrastructure and reliability, data and hardware, and classic ML, DL, CV and NLP.](assets/06-stack.png)

![what a number has to survive — before I believe a result of mine it is load tested at matched concurrency against a naive baseline, judged by a rubric calibrated on human reference and paired against the pre-change run, costed per 1M tokens at matched quality with break-even against real load, and scored on a golden set that predates the change it judges. Tell me the standard you hold and I'll bring numbers that meet it.](assets/07-standard.png)

<sub>GMT+5 · happy to argue about any line above · <a href="https://t.me/Ig0Ro4">telegram</a> · <a href="https://github.com/Isk4R1oT?tab=repositories">all repos</a></sub>
